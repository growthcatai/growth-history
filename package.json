{
	"name": "growth-history",
	"type": "module",
	"version": "0.0.1",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "node scripts/fetch-data.js && astro dev",
		"build": "node scripts/fetch-data.js && astro build",
		"preview": "node scripts/fetch-data.js && astro build && astro preview",
		"astro": "astro",
		"generate-types": "wrangler types",
		"deploy": "node scripts/fetch-data.js && astro build && wrangler deploy",
		"cf-typegen": "wrangler types",
		"seed": "node scripts/seed-local-db.js"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^13.7.0",
		"astro": "^6.4.7",
		"better-auth": "^1.6.19",
		"drizzle-orm": "^0.45.2",
		"html2canvas": "^1.4.1"
	},
	"devDependencies": {
		"wrangler": "^4.100.0"
	},
	"overrides": {
		"vite": "^7"
	}
}
